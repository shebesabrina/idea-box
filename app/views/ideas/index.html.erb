<h3>All them Ideas</h3>

<div id=background >
<ul>
  <% @ideas.each do |idea| %>
  <ol>
    <h3><%= link_to idea.title, user_idea_path(current_user, idea) %></h3>
    <%= idea.description %>
    <%= link_to "Edit", edit_user_idea_path(current_user, idea) %>
    <%= link_to "Delete", user_idea_path(current_user, idea), method: "delete" %>
  </ol>
  <% end %>
</ul>

</div>
